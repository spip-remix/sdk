<?php

function ecrire_fichier_securise(
    $fichier,
    $contenu,
    $ecrire_quand_meme = false,
    $truncate = true
): bool {
    return true;
}

function lire_fichier_securise(
    $fichier,
    &$contenu,
    $options = []
): bool {
	return true;
}
